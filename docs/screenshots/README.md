# Screenshots

Drop PNGs here with exactly these names — the root README already points at them.

| File | Page | Viewport |
|------|------|----------|
| `home.png` | `/` — hero, price list | 1440 × 900 |
| `booking.png` | `/booking` — service, barber, day rail, slot grid | 1440 × 1200 |
| `shop.png` | `/shop` — catalogue with a product card open | 1440 × 1000 |
| `admin.png` | `/admin/appointments` — day book and the booking form | 1440 × 1000 |
| `mobile.png` | `/booking` on a phone width | 390 × 844 |

How: open the page, `F12` → device toolbar for the mobile one, then
`Ctrl+Shift+P` → "Capture full size screenshot".

Keep each file under ~500 KB so the repo stays light.
